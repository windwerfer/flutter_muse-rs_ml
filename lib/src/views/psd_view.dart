import 'package:flutter/material.dart';

/// Power Spectral Density view — placeholder.
class PsdView extends StatelessWidget {
  const PsdView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Power Spectral Density (PSD)\n(FFT visualization will be shown here)'),
    );
  }
}
